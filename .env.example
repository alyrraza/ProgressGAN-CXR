HF_REPO=alyrraza/progressgan-cxr
ENVIRONMENT=production
GRAFANA_PASSWORD=your_secure_password_here

# Set to true for local development to load .pth files from ../models/ directly.
# Set to false (or omit) in production — models stream from HF Hub into memory.
LOCAL_MODELS=true
